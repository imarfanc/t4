# July Checkpoints

## 2026-07-31 — Working state (HEAD: e1e00ef)

Clean working tree on top of `e1e00ef` (sync merge of `main`). Advances the
2026-07-30 baseline (`92ffde1`) via PR #3 (v1 shell + assembly cleanup) and a
run of KV, API-routes, cursor, and sidebar-heading work.

- HEAD: `e1e00ef` Merge branch 'main' of <https://github.com/imarfanc/vt6>
- Prior notable: `3c1b10f` Merge PR #3 `cleanup/frontends26730`; `a94331b` reshape v1 shell + KV admin apps
- Since `92ffde1`: KV explorer/todo apps, API routes docs, cursor file routing, assembly sidebar headings/sections + nested roots, UI collapsible groups, git-flow skill symlink
- Status: tracked (working tree fully clean; this checkpoints file is now committed)
- State: **working** — clean baseline after the PR #3 merge

## 2026-07-30 — Working state (HEAD: 92ffde1)

Clean working tree on top of `92ffde1` (Merge PR #2 `feat-test/deno-desktop1`).
Baseline before further changes.

- HEAD: `92ffde1` Merge pull request #2 from imarfanc/feat-test/deno-desktop1
- Prior notable: `212c84c` add v1 plain-CSS shell, shift daisyUI shell to v2
- Status: tracked (only `_other/checkpoints/` untracked at time of checkpoint)
- State: **working** — verified as a known-good baseline
