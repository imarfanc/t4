# Checkpoints

Short, dated notes about the state of the repo — what was in flight, what was
decided, what to pick up next. One entry per checkpoint, newest first. These are
notes to your future self and to agents starting a cold session; they are not a
changelog.

---

<!-- template:sample -->

## 2026-07-31 — sample checkpoint

- **In flight:** skill symlinking (`vp run skills`) working across all three
  agent targets.
- **Decided:** `_other/` holds everything that does not ship. Nothing under
  `_other/` is ever a deliverable.
- **Next:** wire a real `dev` script once the app exists — the placeholder in
  `package.json` exits 1 on purpose.

> Replace this entry with your own. Delete the file if you do not want
> checkpoints.
